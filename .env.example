# Kopieren nach .env (gitignored) fuer die lokale Entwicklung.
# In Produktion setzt Ansible die Werte aus dem Vault (selfhosted, Rolle padellist).

APP_PASSWORD=padel
SESSION_SECRET=lokales-entwicklungs-secret-bitte-in-prod-ersetzen
DB_PATH=data/padellist.db
PORT=8080
